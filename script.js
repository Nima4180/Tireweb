document.getElementById('language-switcher').addEventListener('change', (event) => {
    const selectedLanguage = event.target.value;
    alert(`Switching to ${selectedLanguage}`);
    // Add functionality for changing website language here.
});
